name: SPECTER VPN Tariff 10 Auto Update

on:
  schedule:
    # 🕐 КАЖДЫЕ 11 ЧАСОВ (с учётом задержки = реал 12ч)
    - cron: '30 */11 * * *'
  workflow_dispatch:  # ✅ Ручной запуск

jobs:
  update-tariff10:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install boto3 requests
    
    - name: Run Tariff 10 Update Script
      env:
        YANDEX_ACCESS_KEY: ${{ secrets.YANDEX_ACCESS_KEY }}
        YANDEX_SECRET_KEY: ${{ secrets.YANDEX_SECRET_KEY }}
      run: |
        python update_tariff10_cloud.py
    
    - name: Success notification
      if: success()
      run: |
        echo "✅ Tariff 10 updated successfully!"
        echo "🔗 https://storage.yandexcloud.net/tariff10/отобранные.txt"
